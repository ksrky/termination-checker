fib(n) =
    if n == 0 then 1
    else if n == 1 then 1
    else fib(n-1) + fib(n-2).

ack(m, n) =
    if m == 0 then 1
    else if n == 0 then ack(m-1,1)
    else ack(m-1, ack(m, n-1)).

gcd(n, m) =
    if m == 0 then n
    else if m > n then gcd(m, n)
    else gcd(n-m, m).
